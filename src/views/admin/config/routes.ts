// Rutas del frontier admin (base del motor de mesas). Sin lógica — solo paths.
export const ROUTES = {
  ADMIN_DASHBOARD: "/admin",
  ADMIN_MESAS: "/admin/mesas",
  ADMIN_RESERVAS: "/admin/reservas",
  ADMIN_CONFIG: "/admin/configuracion",
  ADMIN_COMPONENTS: "/admin/componentes",
} as const;
